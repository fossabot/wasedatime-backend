const sampleDoc = {
    location: {
        method: 'GET',
        name: '',
        path: '',
        statusCode: '500',
        type: 'error'
    },
    properties: JSON.stringify({
        description: "The root resource with the default path specification."
    })
};